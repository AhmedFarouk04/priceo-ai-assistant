// Reserved helper functions for formatting assistant outputs.
// Currently unused by the assistant runtime, but kept as a documented placeholder.
export function formatResponse(value) {
  return value;
}
